# Privacy (AI Education Reader)

本说明描述当前版本的真实架构。它不构成对第三方服务数据处理规则的承诺。

## 本地数据

以下数据默认保存在浏览器本地，不会上传到本项目服务器：

- 支持环境下：大型二进制原文件（原始 PDF、图片附件）主要保存在浏览器 **OPFS**（Origin Private File System），仅当前站点 origin 可访问，不是上传到云端；
- 其余元数据、章节树、会话、API 设置等保存在 IndexedDB；
- 不支持 OPFS 或写入失败时，二进制自动回退为 IndexedDB 内联保存，功能不受影响；

- 会话与消息；
- 图片附件；
- PDF 转换出的页面图片；
- PDF Context metadata（分组来源信息）；
- 本地文档（打开过的原始 PDF 文件）；
- Prompt 配置、会话模式历史、快捷追问和学习成果的来源快照；
- 学习卡片（保存的 AI 回复正文、标题、来源会话与来源 PDF 的文件名/页码快照）；
- API 设置（含 API Key）。

API Key 仅保存在当前浏览器本地；导出备份时**不会**包含 API Key。

## 学习卡片

保存学习卡片是**纯本地操作**：它把某一条已经完成的 AI 回复正文复制为卡片记录，**不会发起任何网络请求**，因此不消耗模型额度，也不会上传任何内容。

卡片只保存文本与结构化来源信息（会话 id、来源 PDF 的 documentId、文件名快照、页码快照），**不保存** PDF 或图片的二进制、object URL 或 base64。删除来源会话或 PDF 不会删除卡片，卡片仍可阅读并保留文件名/页码快照，但跳转会被停用。完整备份 JSON 从 v2.2.0 起包含学习卡片，仍然不包含 API Key。

## PDF

打开一份 PDF 时：**不会因为“打开 PDF”就把整个 PDF 上传到项目服务器。**
PDF.js 在浏览器本地读取与渲染，只有你选择并真正发送的页面图片才会进入 AI 请求。

打开过的 PDF 可以保存在浏览器本地（支持环境下为 OPFS，否则 IndexedDB），供之后的本地文件阅读功能使用（本地文档库）；原始 PDF 不会因此上传到本项目服务器或模型 API。删除浏览器站点数据会一并删除这些本地文件。

## AI 目录识别

使用“AI 识别目录”时，只有你在目录页选择器中**主动勾选的目录页图片**会发送到你配置的 AI API 用于生成目录；完整 PDF 不会因此自动上传。识别结果只是本地草稿，只有你检查并点击保存后才会写入本地；自动识别的层级来自模型、属于不可信输入，需你人工确认后才生效。

## 模型请求

只有你真正发送消息时：选中的图片/PDF 页面以及聊天上下文才会发送到你在设置中配置的 AI API。

Prompt 管理、会话模式、快捷追问和系统协议的定义默认只在当前浏览器本地读取。它们不会因为打开管理界面而发送网络请求；只有实际发送聊天、生成学习成果或执行 AI 目录识别时，相关的已选内容和所需 Prompt 才会进入你配置的模型请求。

备份会保存这些学习配置和历史来源快照，以便在本地恢复；API Key 不会写入备份。旧会话没有模式历史时会明确标记为未知，不会根据当前配置猜测过去使用过的 Prompt。

## 项目服务器

当前 GitHub Pages 版本：**没有本项目自己的应用后端。** 浏览器直连你配置的 API 服务。

## 第三方服务

当你调用 DeepSeek 或其他配置的 endpoint 时：相关数据会进入对应第三方服务，
其数据处理规则（保存、训练、删除等）受该第三方服务自身政策约束。请查阅对应服务商的隐私条款。
本项目不替代任何第三方隐私政策，也不对第三方做“绝不保存/绝不训练/绝对保密”等未经其确认的承诺。

## Analytics

当前版本**不包含**产品分析或广告追踪代码。

## 清除数据

设置中的“清除本地数据”可删除全部本地会话、图片附件、PDF 页面、标注、草稿、本地文档（含原始 PDF Blob）与 API 设置（含 API Key）。
